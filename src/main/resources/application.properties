# Application Configuration
spring.application.name=Win777 Backend

# JWT Configuration
# IMPORTANT: Set a strong secret key in production (minimum 32 characters)
# Example: jwt.secret=YOUR_SECURE_SECRET_KEY_HERE_MINIMUM_32_CHARACTERS_LONG
jwt.secret=${JWT_SECRET:Win777SecretKeyForJWTTokenGenerationAndValidation2024}
jwt.expiration=86400000

# Database Configuration (PostgreSQL)
# Configure these properties for your PostgreSQL database
#spring.datasource.url=jdbc:postgresql://localhost:5432/win777db
#spring.datasource.username=your_username
#spring.datasource.password=your_password
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=false
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
